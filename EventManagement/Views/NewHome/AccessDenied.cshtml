@{
    ViewBag.Title = "Access Denied";
}

<div class="container mt-5 text-center">
    <div class="jumbotron">
        <h2 class="text-danger">Access Denied</h2>
        <p class="text-muted">You do not have the required permissions to access this resource.</p>
        <p><a class="btn btn-primary btn-lg" href="@Url.Action("Dashboard", "NewHome")" role="button">Go back to the home page</a></p>
    </div>
</div>
